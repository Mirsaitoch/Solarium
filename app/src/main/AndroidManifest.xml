<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fin_reg">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Solarium"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Student_java.Student_note"></activity>
        <activity android:name=".Student_java.Student_profile" />
        <activity android:name=".Student_java.StudentMainActivity" />
        <activity android:name=".Teacher_java.Teacher_groups" />
        <activity android:name=".Teacher_java.Teacher_profile" />
        <activity android:name=".Teacher_java.TeacherMainActivity" />
        <activity android:name=".Register" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>